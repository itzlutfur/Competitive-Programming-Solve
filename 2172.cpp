#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x, m, ans;

    while (cin >> x >> m && x != 0 && m != 0)
    {
        ans = m * x;
        cout << ans << endl;
    }

    return 0;
}